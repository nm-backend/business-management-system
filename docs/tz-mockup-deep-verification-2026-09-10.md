# Углублённая сверка код ↔ ТЗ ↔ макеты — 2026-09-10

Метод: 11 изображений макетов из прошлой сессии в воркспейсе не сохранились,
поэтому базой служит экранный перечень макетов (9 листов / 48 окон,
`docs/mockup-screen-audit-2026-09-08.md`) — **каждый его пункт перепроверен по
текущему коду**, а не по тексту документа (документ от 08.09, до правок 1:1,
и статусы в нём устарели). Плюс полный чеклист текстового ТЗ.

---

## 1. Чеклист текстового ТЗ — всё подтверждено кодом

| Пункт ТЗ | Факт | Где |
|---|---|---|
| 3 роли + платформенный супер-админ | ✅ owner/admin/manager/worker + superadmin, изоляция на сервере | `apps/core/permissions.py`, матрица `apps/core/tests_role_matrix_k.py` (34 окна × 5 ролей) |
| Админ без финансов (сервер, не UI) | ✅ 403 на finance/payments/debt_summary/exports + limited-сериализаторы без сумм | `tests_financial_isolation_v2.py` (рекурсивный скан JSON/PDF/Excel), матрица |
| Работник «только своё» | ✅ заказы и задачи по `worker=user`; чужую задачу не принять; confirm/reject/cancel — owner/admin | `apps/orders/views.py:56`, `apps/production/views.py:88,464` |
| Склад сырья и готовой продукции | ✅ RawMaterial/FinishedProduct + ячейки, зоны А–Г, занятость, приход-документы | `apps/warehouse/models.py` |
| Производство только с подтверждением | ✅ сдача → AWAITING_CONFIRMATION → confirm (списание сырья + приход + начисление) / reject | `apps/production/services.py` |
| Задачи: 6 причин отказа | ✅ ровно 6 в модели и в модалке UI + фото-вложение к отказу | `apps/production/models.py:41`, `production.js:369` |
| Заказы: 10 статусов | ✅ 10 в модели; канбан 8 колонок + переходы, список с вкладками | `apps/orders/models.py:23`, `kanban.js:24` |
| Красная карточка при нехватке | ✅ красная полоса + «Материал етишмайди» + блок требований с «Етарли/Етарли эмас» | `orders.js:154–227` |
| Клиенты: auto_archive | ✅ после оплаты и после выдачи заказа | `apps/clients/views.py:359`, `apps/orders/views.py:414` |
| 18 категорий расходов | ✅ ровно 18 | `apps/finance/models.py:19` |
| Аналитика: 16 метрик / 7 периодов | ✅ revenue…low_stock_count (16); today/yesterday/week/month/quarter/year + custom | `apps/reports/services.py:240`, `views.py:55` |
| Админ — аналитика без денег | ✅ operational-вариант, тест-скан | `get_admin_operational_analytics` |
| Квартальные ×2 | ✅ владельцу financial (с «Рентабеллик %»), админу/менеджеру operational | `QuarterlyReportView` |
| Экспорт по ролям + опции | ✅ finance/company-data — owner; stock/orders/work — без денег; тумблеры графкики/изоҳлар/детализация | `reports/views.py`, `settings.js:110` |
| Чат-матрица | ✅ работники скрыты без can_see_other_workers; работник → владельцу только с can_write_to_owner | `apps/messaging/serializers.py:195` |
| uz основной / ru / ky, тексты только в локалях | ✅ паритет 936 ключей ×3, два стража i18n | `apps/core/tests_i18n_keys_k.py` |
| Вход без утечек служебного текста | ✅ исправлено, тесты рендера | `apps/accounts/tests_login_leak_k.py` |

## 2. Расхождения из аудита 08.09 — текущий статус (13 пунктов)

| # | Пункт | Статус | Подтверждение в коде |
|---|---|---|---|
| 1 | Рецепт: размер, толщина, выход партии | ✅ | `Recipe.size/thickness/output_quantity/code` (`warehouse/models.py:746`) |
| 2 | Плановое количество в задаче | ✅ | `Task.planned_quantity/planned_unit`, автозаполнение из заказа (`production/views.py:148`), показ в карточке (`production.js:210`) |
| 3 | Приход документом на несколько материалов | ✅ | `GoodsReceipt` + строки, идемпотентность; UI «строки + добавить» (`warehouse.js:1192`) |
| 4 | Цель расхода + привязка к заказу | ✅ | `StockMovement.purpose` (5 значений) + `related_order`; показ в истории (`warehouse.js:873`) |
| 5 | Вкладки и итоги истории движений | ✅ | серверные категории (`warehouse/views.py:782`), вкладки Ҳаммаси/Келган/Ишлатилган/Қайтарилган + «Жами келган/чиққан/қолдиқ» (`warehouse.js:826–840`) |
| 6 | Счётчики шапки и суммы в чипах типов | ✅ | `types_count/materials_count/type_totals` в summary; чипы показывают остаток в м² (`warehouse.js:497`) |
| 7 | Долги: бакеты «1–7/8–14/15+/муддати бор» | ✅ | `debt_summary` по payment_due_date (`clients/views.py:186`) |
| 8 | Тип клиента, ответственный, вкладки карточки | ✅ | поля + фильтры; карточка: Умумий/Заказлар/Тўловлар/Қарзлар (`clients.js:250`) |
| 9 | Уведомления: архив и группировка | ✅ | `?is_archived=true`, группировка по категориям (`messages.js:771–812`) |
| 10 | Тумблеры экспорта (графики/изоҳлар/детализация) | ✅ | API `charts/notes/detailed` (`reports/views.py:386`), UI в Настройках (`settings.js:110–119`) |
| 11 | Управление сессиями | ✅ | `/accounts/me/sessions/` + revoke, UI «Сеанслар» (`settings.js:637`) |
| 12 | Резервные копии владельцу | ✅ (по решению) | платформенный backup — супер-админ; владельцу — выгрузка СВОЕЙ компании `export/company-data/` + аудит |
| 13 | Рентабеллик % / выручка по источникам | ⚠️ | «Рентабеллик 71.2 %» считается (`services.py:670`); пончик «Манбалар бўйича даромад» — **нет**: в системе нет сущности «хизмат», классификация сотув/хизмат/бошқа невозможна без продуктового решения |

Экран «Сотувлар», уведомления WORK_ACCRUED/MATERIAL_SHORTAGE/REPORT_READY,
фото к отказу, зона «Г», «задачи сегодня», «Умумий қиймат» — сделаны в этой
сессии и перепроверены своими тестами (30) и матрицей.

## 3. Мелкие наблюдения этого прохода (не блокируют)

1. Вкладки списка заказов: `all/new/sent_to_worker/in_progress/awaiting_confirmation/ready/delivered/cancelled/неоплаченные` — нет отдельных вкладок `awaiting_material` и `worker_refused`. Статусы существуют, видны в карточках и канбане, доступны через API-фильтр.
2. «Ойлар бўйича: текущий vs прошлый» — покрыто дельтами-процентами на карточках и 6-месячным графиком (выручка + чистая прибыль); отдельных сдвоенных столбцов «этот vs прошлый» нет.
3. Строка «Жами · Қиймати» под таблицей материалов заменена карточками сводки над списком (функционально эквивалентно).

## 4. Требует вашего решения (намеренно не тронуто)

1. **Минимум фото при сдаче работы** — макеты: «камида 1–2»; сейчас правило мягкое (работа без фото принимается, админ видит пометку). Ужесточить?
2. **«Бугунги иш 18 соат»** — учёт часов противоречит оплате за единицу продукции из ТЗ. Не делал.
3. **«+ Янги роль қўшиш»** — противоречит ТЗ («ровно три типа аккаунтов»). Не делал.
4. **Пончик «Манбалар бўйича даромад»** — нужен смысл: если «источники» = оплаты с привязкой к заказу vs прочие поступления — сделаю; если нужен тип «услуга» — нужна сущность.
5. **Бэкап владельцу** — если выгрузки company-data мало и нужен именно архив БД — обсуждение (это данные платформы).

## 5. Прогоны после сверки

- Django: **1567 OK** (62 skip — только PostgreSQL-тесты, локально нет PG);
- vitest: **104 OK**; `manage.py check` — без замечаний; миграции полные;
- i18n-паритет: 936 ключей × uz_cyrl/ru/ky.

Код в этот проход не менялся (расхождений, требующих правки без вашего решения, не найдено).
